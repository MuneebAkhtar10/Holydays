import { CatalogGrid } from "@/components/CatalogGrid";

export default function AttractionsPage() {
  return (
    <CatalogGrid
      kind="ATTRACTION"
      title="Ziyarat"
      urdu="زیارت"
      blurb="Shrine plans for Saudi Arabia, Iraq, and Iran. Partners add these; they fold into a hotel package at checkout."
    />
  );
}
