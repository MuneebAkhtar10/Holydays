import { CatalogGrid } from "@/components/CatalogGrid";

export default function TaxisPage() {
  return (
    <CatalogGrid
      kind="TAXI"
      title="Taxis"
      urdu="کرایہ"
      blurb="Pick a trip with a real itinerary — Karbala to Kufa, Najaf to Karbala, and the rest. Rate is per person first; private books the whole vehicle. Add it from a hotel package."
    />
  );
}
